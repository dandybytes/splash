import React from "react";
import "./Message.css";

const Message = () => {
    return (
        <p className="Message">
            Paint the entire board in a single color before running out of moves to win the game.
            Click on the right-side buttons to change colors.
        </p>
    );
};

export default Message;
